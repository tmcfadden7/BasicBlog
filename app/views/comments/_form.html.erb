<%= form_with(model: @post, local: true) do |form| %>
  <p>
    <%= current_user.email %>
  </p>
  <p>
    <%= form.label :comment %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
